export class Products {
    constructor(public name: string, public description: string, public current_stock: number, public category: string, public price: number, public shop: string, public stroked_price: number, public min: number, public max: number, public child_category: number) {}
}