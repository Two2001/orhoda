export class UpdateProduct {
    constructor(public name: string, public description: string, public currentstock: number, public category: string, public price: number, public strokedprice: number) {}
}